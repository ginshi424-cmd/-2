
# 🚀 Инструкция: Размещение на хостинге

---

## 💻 Шаг 1: Подготовка папок

У тебя сейчас все файлы лежат в одной куче. Чтобы на сервере был порядок, давай убедимся, что структура такая.

Создай папку `backend` внутри своего проекта и перемести туда 3 файла сервера.

**Итоговая структура должна быть такой:**

```text
📁 F1_PROJECT (Твоя главная папка)
├── 📁 backend              <-- ПАПКА ДЛЯ СЕРВЕРА
│   ├── server.js           (Перемести сюда)
│   ├── package.json        (Тот, который для сервера)
│   └── .env                (Перемести сюда)
├── 📁 components           (Оставь здесь)
├── 📁 services             (Оставь здесь)
├── App.tsx                 (Оставь здесь)
├── index.tsx               (Оставь здесь)
├── index.html              (Оставь здесь)
├── vite.config.ts          (Оставь здесь)
├── package.json            (Тот, который для сайта)
└── table.sql               (Твой файл с базой данных)
```

---

## 🛠 Шаг 2: Настройка Сервера (VPS)

Зайди на свой сервер через терминал (PuTTY) и выполняй команды по очереди:

```bash
# 1. Обновляем систему
sudo apt update

# 2. Устанавливаем Node.js (версия 18)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 3. Устанавливаем Базу Данных и Веб-сервер
sudo apt install mysql-server nginx -y

# 4. Устанавливаем менеджер процессов (чтобы сайт работал вечно)
sudo npm install -g pm2
```

---

## 🗄️ Шаг 3: Настройка Базы Данных

1.  Зайди в MySQL: `sudo mysql`
2.  Выполни этот блок команд (скопируй и вставь всё сразу):
    ```sql
    CREATE DATABASE f1_tickets;
    CREATE USER 'gp1user'@'localhost' IDENTIFIED BY 'SecretPass';
    GRANT ALL PRIVILEGES ON f1_tickets.* TO 'gp1user'@'localhost';
    FLUSH PRIVILEGES;
    EXIT;
    ```
    *(Пароль `SecretPass` можешь поменять на свой, но не забудь поменять его и в файле `.env`)*.

---

## 📤 Шаг 4: Загрузка файлов на сервер

1.  Открой **FileZilla**.
2.  Подключись к серверу (IP, root, пароль).
3.  Перейди в папку `/var/www` (нажми `..` сверху пока не дойдешь до корня, потом `var`, потом `www`).
4.  Создай там папку `f1-app`.
5.  **ПЕРЕТАЩИ ВСЁ** из своей папки `F1_PROJECT` внутрь `/var/www/f1-app`.

---

## ⚙️ Шаг 5: Запуск

Возвращаемся в черный терминал (PuTTY).

**1. Заливаем твою таблицу в базу:**
```bash
mysql -u gp1user -p f1_tickets < /var/www/f1-app/table.sql
```
*(Попросит пароль — введи `SecretPass`)*

**2. Запускаем Бэкенд (Сервер):**
```bash
cd /var/www/f1-app/backend
npm install
pm2 start server.js --name "f1-api"
```

**3. Собираем Фронтенд (Сайт):**
```bash
cd /var/www/f1-app
npm install
npm run build
```

---

## 🔗 Шаг 6: Финальная настройка (Nginx)

1.  Открываем файл настройки: `nano /etc/nginx/sites-available/default`
2.  Стираем всё (держи Ctrl+K) и вставляем этот текст:

```nginx
server {
    listen 80;
    server_name _;

    # Путь к твоему сайту
    root /var/www/f1-app/dist;
    index index.html;

    # Главная страница
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API (Запросы к базе)
    location /api/ {
        proxy_pass http://localhost:3001/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```
3.  Сохраняем: нажми `Ctrl+O`, затем `Enter`, затем `Ctrl+X`.
4.  Перезагружаем Nginx: `systemctl restart nginx`

**ГОТОВО!** Открывай IP своего сервера в браузере.
